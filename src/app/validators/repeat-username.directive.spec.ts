import { RepeatUsernameDirective } from './repeat-username.directive';

describe('RepeatUsernameDirective', () => {
  it('should create an instance', () => {
    const directive = new RepeatUsernameDirective();
    expect(directive).toBeTruthy();
  });
});
